import { View, LabeledFieldView, SplitButtonView, ToolbarSeparatorView, createDropdown, ToolbarView } from 'ckeditor5/src/ui.js';
import { type Locale } from 'ckeditor5/src/utils.js';

export default class LookupErrorView extends View {

  constructor(locale: Locale) {
    super(locale);

  }
}
